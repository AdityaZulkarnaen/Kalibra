/**
 * The indexer as a library, for the demo script and for tests. Importing `index.ts` would
 * run the CLI as a side effect; this entry point does not.
 */
export * from './config.js';
export * from './ingest.js';
export * from './pipeline.js';
