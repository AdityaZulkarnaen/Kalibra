/** Where the source and its documents live. One place, so a fork changes one line. */
export const REPO_URL = 'https://github.com/AdityaZulkarnaen/Kalibra';

export const docLink = (path: string): string => `${REPO_URL}/blob/main/${path}`;
